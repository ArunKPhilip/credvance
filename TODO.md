# Vercel deployment TODO

- [ ] Create a Vercel-compatible server entry point for the existing Express API.
- [ ] Add Vercel builder config so `npm run build` outputs the right files.
- [ ] Ensure frontend is built and served by the API server (or configure rewrites).
- [ ] Add Vercel env var mapping guidance (PORT, ADMIN_API_KEY, PII_HASH_SALT, FIREBASE_*).
- [ ] Test locally with a Vercel-like run (build + start).
- [ ] Provide the Vercel deployment steps (Project -> Import -> Build & Output settings).

