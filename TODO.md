# Vercel deployment TODO

- [x] Add Vercel-compatible routing config (`vercel.json`) for SPA + API paths.
- [x] Ensure Express server is runnable on Vercel via Node runtime (configured in `vercel.json`).
- [x] Ensure build outputs frontend into `dist/frontend` and API into `dist/backend` (already matches repo scripts).
- [x] Add Vercel env var mapping guidance (see `vercel.deploy.md`).
- [x] Test build + start locally (Vercel-like) and verify SPA routing health endpoint.
- [x] Provide deployment steps for Vercel + Godaddy domain (see `vercel.deploy.md`).

